<Window x:Class="IRacingOverlay.App.RelativeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="iRacing Overlay - Relative"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI Variable Display, Segoe UI"
        MouseLeftButtonDown="OnDragWindow">

    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Exit" Click="OnExit" />
        </ContextMenu>
    </Window.ContextMenu>

    <Border Width="316"
            Padding="14,10,14,12"
            CornerRadius="12"
            Background="{StaticResource PanelBackground}"
            BorderBrush="{StaticResource PanelBorder}"
            BorderThickness="1">
        <StackPanel>

            <DockPanel Margin="0,0,0,6">
                <Ellipse Width="8" Height="8" VerticalAlignment="Center" DockPanel.Dock="Left">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="{StaticResource Warning}" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                    <Setter Property="Fill" Value="#4ADE80" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <TextBlock Text="{Binding ConnectionStatus}"
                           DockPanel.Dock="Right"
                           VerticalAlignment="Center"
                           Style="{StaticResource Label}" />
                <TextBlock Text="RELATIVE"
                           Margin="8,0,0,0"
                           VerticalAlignment="Center"
                           FontSize="11"
                           FontWeight="Bold"
                           Foreground="{StaticResource Accent}" />
            </DockPanel>

            <!-- session strip: session/time · brake bias · temps · wetness · incidents -->
            <DockPanel Margin="0,0,0,8">
                <TextBlock Text="{Binding SessionText}"
                           DockPanel.Dock="Left"
                           VerticalAlignment="Center"
                           Style="{StaticResource Label}"
                           Foreground="{StaticResource TextPrimary}" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding BrakeBiasText}"
                               Margin="8,0,0,0"
                               Style="{StaticResource Label}"
                               Visibility="{Binding HasBrakeBias, Converter={StaticResource BoolToVis}}" />
                    <TextBlock Text="{Binding TrackTempText}" Margin="8,0,0,0" Style="{StaticResource Label}" />
                    <TextBlock Text="{Binding AirTempText}" Margin="8,0,0,0" Style="{StaticResource Label}" />
                    <TextBlock Text="{Binding WetnessText}"
                               Margin="8,0,0,0"
                               FontSize="10"
                               FontWeight="Bold"
                               Foreground="{StaticResource Warning}"
                               Visibility="{Binding IsWet, Converter={StaticResource BoolToVis}}" />
                    <TextBlock Text="{Binding IncidentsText}" Margin="8,0,0,0" Style="{StaticResource Label}" />
                </StackPanel>
            </DockPanel>

            <Border Height="1" Background="{StaticResource Separator}" Margin="0,0,0,6" />

            <ItemsControl ItemsSource="{Binding Rows}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Height="24" CornerRadius="6" Padding="6,0" Margin="0,1">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPlayer}" Value="True">
                                            <Setter Property="Background" Value="#3338BDF8" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsVisible}" Value="False">
                                            <Setter Property="Visibility" Value="Hidden" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20" />
                                    <ColumnDefinition Width="34" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="44" />
                                    <ColumnDefinition Width="34" />
                                    <ColumnDefinition Width="48" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="{Binding PositionText}"
                                           FontSize="11"
                                           Foreground="{StaticResource TextMuted}" />

                                <TextBlock Grid.Column="1"
                                           Text="{Binding CarNumberText}"
                                           FontSize="11"
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource TextMuted}" />

                                <TextBlock Grid.Column="2"
                                           Text="{Binding Name}"
                                           FontSize="13"
                                           FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding LapDifference}" Value="LapAhead">
                                                    <Setter Property="Foreground" Value="{StaticResource LapAheadText}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding LapDifference}" Value="LapBehind">
                                                    <Setter Property="Foreground" Value="{StaticResource LapBehindText}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding InPits}" Value="True">
                                                    <Setter Property="Opacity" Value="0.55" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <TextBlock Grid.Column="3"
                                           Text="{Binding License}"
                                           FontSize="11"
                                           Foreground="{StaticResource TextMuted}" />

                                <TextBlock Grid.Column="4"
                                           Text="{Binding IRatingText}"
                                           FontSize="11"
                                           Foreground="{StaticResource TextMuted}" />

                                <StackPanel Grid.Column="5"
                                            Orientation="Horizontal"
                                            HorizontalAlignment="Right">
                                    <TextBlock Text="PIT"
                                               Margin="0,0,5,0"
                                               VerticalAlignment="Center"
                                               FontSize="9"
                                               FontWeight="Bold"
                                               Foreground="{StaticResource Warning}"
                                               Visibility="{Binding InPits, Converter={StaticResource BoolToVis}}" />
                                    <TextBlock Text="{Binding DeltaText}"
                                               FontSize="12"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource TextPrimary}" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </Border>
</Window>
